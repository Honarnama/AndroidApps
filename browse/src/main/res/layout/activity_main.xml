<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent" android:layout_height="match_parent"
        tools:context="net.honarnama.browse.MainActivity"
        android:layout_gravity="right"
        android:gravity="right"
        >

    <android.support.v7.widget.Toolbar
            android:id="@+id/browse_toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:theme="@style/ActionBarThemeOverlay"
            app:popupTheme="@style/ActionBarThemeOverlayPopup"
            android:layout_gravity="right"
            android:gravity="right"
            android:layoutDirection="rtl"
            android:elevation="4dp"/>

    <FrameLayout
            android:layout_below="@id/browse_toolbar"
            android:id="@+id/browse_drawer_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <!-- the layout which will be the content of the activity (which will be hosted inside the drawer (NOT the list of the drawer)) -->
        <FrameLayout
                android:id="@+id/browse_frame_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
        </FrameLayout>
    </FrameLayout>
</RelativeLayout>
