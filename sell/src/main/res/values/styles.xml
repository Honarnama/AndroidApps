<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Toolbar_Theme">
        <!--
        <item_row name="colorControlNormal">#ffffff</item_row>
        <item_row name="android:textStyle">bold</item_row>
        <item_row name="android:textColor">#ffffff</item_row>
        -->
    </style>

    <style name="sell_full_width_button" parent="full_width_button">

        <item name="android:background">@color/amber</item>
        <item name="android:textColor">@color/white</item>

    </style>

    <style name="greenEditTextTheme">
        <!-- Used for the bottom line when not selected / focused -->
        <item name="colorControlNormal">@color/white</item>
        <!-- colorControlActivated & colorControlHighlight use the colorAccent color by default -->
    </style>
    <style name="MyMaterialTheme" parent="MyMaterialTheme.Base">

    </style>

    <style name="MyMaterialTheme.Base" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
</resources>